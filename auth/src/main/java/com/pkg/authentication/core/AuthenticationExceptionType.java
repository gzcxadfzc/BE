package com.pkg.authentication.core;

public enum AuthenticationExceptionType {

    INVALID_CREDENTIAL,
    EXPIRED_CREDENTIAL,
    MISSING_CREDENTIAL,
    INVALID_CLAIM,
    BAD_CREDENTIAL,
    UNKNOWN
    ;
}
